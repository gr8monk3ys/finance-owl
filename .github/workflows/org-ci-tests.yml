name: ci-tests

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions: read-all

jobs:
  ci-tests-minimum:
    runs-on: ubuntu-latest
    steps:
      - name: Cost guard
        run: echo "Duplicate minimum CI coverage is handled by the main CI workflow."
