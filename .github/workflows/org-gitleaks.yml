name: org-gitleaks

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions: read-all

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - name: Cost guard
        run: echo "Secret scanning is handled by security.yml on scheduled and default-branch runs."
