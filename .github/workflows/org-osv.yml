name: org-osv

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions: read-all

jobs:
  osv:
    runs-on: ubuntu-latest
    steps:
      - name: Cost guard
        run: echo "OSV scanning is handled by scheduled and default-branch security workflows."
