name: org-precommit

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions: read-all

jobs:
  precommit:
    runs-on: ubuntu-latest
    steps:
      - name: Cost guard
        run: echo "Pre-commit coverage is handled by the main CI workflow."
