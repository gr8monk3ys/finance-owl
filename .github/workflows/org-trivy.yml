name: org-trivy

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions: read-all

jobs:
  trivy:
    runs-on: ubuntu-latest
    steps:
      - name: Cost guard
        run: echo "Trivy scanning is handled by security.yml on scheduled and default-branch runs."
