name: org-trufflehog

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions: read-all

jobs:
  trufflehog:
    runs-on: ubuntu-latest
    steps:
      - name: Cost guard
        run: echo "TruffleHog scanning is handled by scheduled and default-branch security workflows."
