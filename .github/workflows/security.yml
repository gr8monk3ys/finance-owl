name: Security

on:
  schedule:
    # Weekly on Monday at 08:00 UTC
    - cron: "0 8 * * 1"
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: security-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  NODE_VERSION: "20"
  PNPM_VERSION: "9.15.0"

jobs:
  # ──────────────────────────────────────────────
  # Dependency audit
  # ──────────────────────────────────────────────
  audit:
    name: Dependency Audit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: PR cost guard
        if: github.event_name == 'pull_request'
        run: echo "Skipping full dependency audit on pull requests; scheduled and default-branch runs still execute it."

      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        if: github.event_name != 'pull_request'

      - uses: pnpm/action-setup@41ff72655975bd51cab0327fa583b6e92b6d3061
        if: github.event_name != 'pull_request'
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        if: github.event_name != 'pull_request'
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        if: github.event_name != 'pull_request'
        run: pnpm install --frozen-lockfile

      - name: Run pnpm audit (production)
        if: github.event_name != 'pull_request'
        run: pnpm audit --prod
        continue-on-error: true

      - name: Run pnpm audit (all)
        if: github.event_name != 'pull_request'
        run: pnpm audit
        continue-on-error: true

  # ──────────────────────────────────────────────
  # Dependency review (PRs only)
  # ──────────────────────────────────────────────
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Cost guard
        run: echo "Dependency review is disabled on pull requests during the GitHub Actions cost reduction pass."

  # ──────────────────────────────────────────────
  # CodeQL analysis
  # ──────────────────────────────────────────────
  codeql:
    name: CodeQL Analysis
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: [javascript-typescript]
    steps:
      - name: PR cost guard
        if: github.event_name == 'pull_request'
        run: echo "Skipping full CodeQL analysis on pull requests; scheduled and default-branch runs still execute it."

      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        if: github.event_name != 'pull_request'

      - name: Initialize CodeQL
        if: github.event_name != 'pull_request'
        uses: github/codeql-action/init@45580472a5bb82c4681c4ac726cfdb60060c2ee1
        with:
          languages: ${{ matrix.language }}
          queries: security-extended

      - name: Autobuild
        if: github.event_name != 'pull_request'
        uses: github/codeql-action/autobuild@45580472a5bb82c4681c4ac726cfdb60060c2ee1

      - name: Perform CodeQL analysis
        if: github.event_name != 'pull_request'
        uses: github/codeql-action/analyze@45580472a5bb82c4681c4ac726cfdb60060c2ee1
        with:
          category: /language:${{ matrix.language }}

  # ──────────────────────────────────────────────
  # SAST scanning (Semgrep)
  # ──────────────────────────────────────────────
  sast:
    name: SAST (Semgrep)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      security-events: write
    container:
      image: semgrep/semgrep
    steps:
      - name: PR cost guard
        if: github.event_name == 'pull_request'
        run: echo "Skipping full Semgrep SAST on pull requests; scheduled and default-branch runs still execute it."

      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        if: github.event_name != 'pull_request'

      - name: Run Semgrep
        if: github.event_name != 'pull_request'
        run: semgrep ci --sarif --output=semgrep-results.sarif
        env:
          SEMGREP_RULES: >-
            p/default
            p/typescript
            p/nodejs
            p/secrets
            p/owasp-top-ten

      - name: Upload SARIF results
        if: github.event_name != 'pull_request' && always()
        uses: github/codeql-action/upload-sarif@45580472a5bb82c4681c4ac726cfdb60060c2ee1
        with:
          sarif_file: semgrep-results.sarif

  # ──────────────────────────────────────────────
  # Secret scanning (Gitleaks)
  # ──────────────────────────────────────────────
  secrets-scan:
    name: Secret Scanning
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: PR cost guard
        if: github.event_name == 'pull_request'
        run: echo "Skipping full secret scan on pull requests; scheduled and default-branch runs still execute it."

      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        if: github.event_name != 'pull_request'
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        if: github.event_name != 'pull_request'
        uses: gitleaks/gitleaks-action@ff98106e4c7b2bc287b24eaf42907196329070c7
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ──────────────────────────────────────────────
  # Docker image vulnerability scan (main only)
  # ──────────────────────────────────────────────
  docker-scan:
    name: Docker Image Scan (${{ matrix.image.name }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        image:
          - name: backend
            dockerfile: docker/Dockerfile.backend
          - name: frontend
            dockerfile: docker/Dockerfile.frontend
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Build image for scanning
        env:
          DOCKERFILE: ${{ matrix.image.dockerfile }}
        run: docker build -f "$DOCKERFILE" -t scan-target:latest .

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@e368e328979b113139d6f9068e03accaed98a518
        with:
          image-ref: scan-target:latest
          format: sarif
          output: trivy-results.sarif
          severity: CRITICAL,HIGH

      - name: Upload Trivy SARIF results
        uses: github/codeql-action/upload-sarif@45580472a5bb82c4681c4ac726cfdb60060c2ee1
        if: always()
        with:
          sarif_file: trivy-results.sarif
