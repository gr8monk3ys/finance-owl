name: semgrep

on:
  push:
    branches:
      - main
  pull_request:

permissions: read-all

jobs:
  semgrep:
    runs-on: ubuntu-latest
    steps:
      - name: Cost guard
        run: echo "Semgrep is handled by security.yml on scheduled and default-branch runs."
